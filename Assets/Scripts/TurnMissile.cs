using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurnMissile : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.rotation = new Quaternion(0, 180, 0, 0);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
